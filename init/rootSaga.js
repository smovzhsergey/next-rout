//Core
import { all, call } from 'redux-saga/effects';

//Watchers
// import { watchProfile } from '../sagas/profile/saga/watchers';

function* rootSaga () {
    // yield all([call(watchProfile)]);
}

export default rootSaga
